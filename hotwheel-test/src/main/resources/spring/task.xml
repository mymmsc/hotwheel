<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
         http://www.springframework.org/schema/task
        http://www.springframework.org/schema/task/spring-task-4.0.xsd">
    <bean id="propertyConfigurer"
          class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:/runtime.properties</value>
            </list>
        </property>
    </bean>

    <!-- ==================== 业务数据 =========================== -->
    <bean id="CollectServiceTask" class="com.jiedaibao.exchange.task.CollectionTask">
        <property name="taskSwitch" value="${task.overdue}"/>
        <property name="taskName" value="${task.overdue.name}"/>
    </bean>
    <task:scheduled-tasks>
        <task:scheduled ref="CollectServiceTask" method="doTask" cron="${batch.cron}" />
    </task:scheduled-tasks>

    <bean id="OverdueServiceTask" class="com.jiedaibao.exchange.task.DebtServiceTask">
        <property name="taskSwitch" value="${task.service}"/>
        <property name="taskName" value="${task.service.name}"/>
    </bean>
    <task:scheduled-tasks>
        <task:scheduled ref="OverdueServiceTask" method="doTask" cron="${batch.cron}" />
    </task:scheduled-tasks>
    <!-- ==================== 修复数据 =========================== -->
    <bean id="fixDataTask" class="com.jiedaibao.exchange.task.FixDataTask">
        <property name="taskSwitch" value="${task.fixData}"/>
        <property name="taskName" value="${task.fixData.name}"/>
    </bean>
    <task:scheduled-tasks>
        <task:scheduled ref="fixDataTask" method="doTask" cron="${batch.cron}" />
    </task:scheduled-tasks>
</beans>